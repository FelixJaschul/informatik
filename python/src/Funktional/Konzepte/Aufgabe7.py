# Gegeben ist das Ergebnis 10.
#
# Finde mindestens drei verschiedene reduce-Ausdrücke (mit beliebigen Listen und Funktionen), die dieses Ergebnis liefern.
#
# Beispiel:
#
# reduce(lambda x, y: x + y, [1, 2, 3, 4])  # ergibt 10